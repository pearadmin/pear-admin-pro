<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.pearadmin.pro.modules.sys.repository.SysPostRepository">

    <select id="selectList" resultType="SysPost">
        select * from sys_post sp
        <where>
            <if test="request.name!=null and request.name!=''">
               and sp.name like concat('%', #{request.name}, '%')
            </if>
            <if test="request.code!=null and request.code!=''">
                and sp.code like concat('%', #{request.code}, '%')
            </if>
        </where>
    </select>
    
</mapper>